[build]
builder = "RAILPACK"
buildCommand = "./gradlew bootJar --no-daemon"

[deploy]
startCommand = "java -jar build/libs/oms-backend-1.0.0.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
